# Core data
pandas>=2.0
pyarrow

# BigQuery
google-cloud-bigquery
google-auth
google-auth-oauthlib

# HTTP / OAuth
requests
requests-oauthlib

# dbt — pin to 1.11.x to match local dev and package-lock.yml format
dbt-bigquery>=1.11.3,<1.12

# Quality
ruff
pytest
pytest-cov

# Utilities
python-dotenv
